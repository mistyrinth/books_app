<% @user ||= current_user %>
<div class="stats">
  <a href="<%= follows_user_path %>">
    <strong id="following" class="stat">
      <%= @user.followings.count %>
    </strong>
    <%= t('.Followings') %>
  </a>
  |
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    <%= t('.Followers') %>
  </a>
</div>